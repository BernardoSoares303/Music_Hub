{
  "name": "music-hub",
  "version": "1.0.0",
  "scripts": {
    "server": "json-server --watch db/db.json --static public --port 3000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}